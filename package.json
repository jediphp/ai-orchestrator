{
  "name": "remote-dev-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Remote AI Developer Agent monorepo",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm build && node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.12.1"
}
